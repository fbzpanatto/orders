export interface PersonAddresses {
  person_id: number,
  add_street?: string,
  add_number?: string,
  add_zipcode?: string,
  add_city?: string,
  add_neighborhood?: string,
  created_at?: Date,
  updated_at?: Date
}